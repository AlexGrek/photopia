apiVersion: v1
kind: Service
metadata:
  name: {{ include "photopia.fullname" . }}
  labels:
    {{- include "photopia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "photopia.selectorLabels" . | nindent 4 }}
